<%= bootstrap_form_for(@zone,layout: :horizontal) do |f| %>
    <%= f.text_field :name %>
    <%= f.text_area :comments %>
    <%= f.collection_select :employee_id, Employee.all, :id,:name,:prompt => "Select zone Engineer"%>
    <%= f.primary%>
<% end %>